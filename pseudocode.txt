input keyword
get_similar(keyword)
get_translation(keyword)

get_similar(keyword):
    similar = get_sinonim(keyword)
    for word in similar:
        while count < 10:
            for line in quran:
                if word in line:
                    count++
                    print line
                    if get_translation(word_of_translation)

get_translation(keyword):
    while next_word != null:
        word_of_translation = next_word
            if keyword == word_of_translation:
                print translation
            
get_sinonim()